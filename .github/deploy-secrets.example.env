# Copy values into GitHub → Settings → Secrets and variables → Actions → New repository secret
# Do NOT commit real passwords to git.

FTP_SERVER=192.249.118.80
FTP_USERNAME=pos@testv3.websitedemolynk.com
FTP_PASSWORD=your-ftp-password-here
# Leave empty or set to / if the FTP account root is already the /pos folder on the server
FTP_SERVER_DIR=/
