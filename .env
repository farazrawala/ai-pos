# Shared defaults (all environments). Mode-specific files override these.
VITE_APP_NAME=AI POS
# Local — XAMPP / machine backend (npm run dev:local)
VITE_APP_ENV=local
VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://localhost:8000
VITE_API_MEDIA_ORIGIN=http://localhost:8000
    